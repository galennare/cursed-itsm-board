import React from "react";
import { Button } from "react-bootstrap";

/*
    This component can be set aside for now because it is
    not a key component of our MVP. We can return to this
    component at a later time in the project.
*/

export function Preferences(): JSX.Element {
    return (
        <span>
            <Button>Preferences</Button>
        </span>
    );
}
