import React from "react";
import { Button } from "react-bootstrap";

export function ViewMyWork(): JSX.Element {
    return (
        <span>
            <Button>View My Work</Button>
        </span>
    );
}
