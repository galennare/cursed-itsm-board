import React from "react";
import { Button } from "react-bootstrap";

export function DropDownLogIn(): JSX.Element {
    return (
        <span>
            <Button>Logged in as User</Button>
        </span>
    );
}
